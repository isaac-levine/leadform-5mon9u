# API Configuration
# Public URL for API endpoints (required)
# Security Level: Public
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# WebSocket Configuration
# WebSocket server URL for real-time updates (required)
# Security Level: Public
NEXT_PUBLIC_WS_URL=ws://localhost:3000

# Application Configuration
# Public URL of the web application (required)
# Security Level: Public
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Authentication Configuration
# NextAuth.js base URL for authentication (required)
# Security Level: Private - Do not expose in client-side code
NEXTAUTH_URL=http://localhost:3000

# NextAuth.js secret key for JWT encryption (required)
# Security Level: Secret - Use a strong random value in production
# Minimum length: 32 characters
NEXTAUTH_SECRET=your-nextauth-secret-key

# Google OAuth 2.0 Credentials (optional)
# Security Level: Secret - Never commit actual values
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Analytics Configuration (optional)
# Security Level: Public
NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Feature Flags
# Multi-Factor Authentication toggle (optional)
# Security Level: Public
# Valid values: true, false
NEXT_PUBLIC_ENABLE_MFA=false

# Maintenance Mode toggle (optional)
# Security Level: Public
# Valid values: true, false
NEXT_PUBLIC_MAINTENANCE_MODE=false

# NOTE: This is an example configuration file
# 1. Copy this file to .env.local for development
# 2. Never commit actual .env files to version control
# 3. Use proper secret management in production
# 4. Rotate secrets regularly following security best practices
# 5. Ensure all required variables are set before deployment